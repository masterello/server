plugins {
    id("org.jetbrains.kotlin.jvm") version "1.9.25"
}

dependencies {
    implementation project(":category-contract")
}

bootJar {
    enabled = false  // Disable the bootJar task in non-application modules
}